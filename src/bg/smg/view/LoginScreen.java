package bg.smg.view;

import javax.swing.*;

public class LoginScreen extends JFrame {

    public LoginScreen(){
        setTitle("Login");
        setSize(300,400);

        setDefaultCloseOperation(EXIT_ON_CLOSE);
        setVisible(true);
    }
}
